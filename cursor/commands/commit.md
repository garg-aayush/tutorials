Commit the staged changes to git using github cli

## Rules

- Only commit the staged changes, if there are no staged changes, do not commit and ask the user to stage the changes first.
- Reference the files that were changed in the commit message
- Write a short description of the changes as a single line message without any newlines
- Use the present tense and active voice